# 프론트엔드
node_modules
dist
dist-ssr
*.local

# Rust / Tauri
src-tauri/target
target

# 크래시 덤프(cygwin/msys 도구가 남긴다)
*.stackdump

# 에디터
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 빌드 시점 설정, 절대 커밋 금지 — 견본은 .env.example
.env
.env.*
!.env.example

# 내 맘
*.bat

# 셸 확장 빌드 산출물, shellext 의 build.bat 이 Z: 배포 폴더로 옮긴다
shellext/ZipManiaShell.dll
shellext/ZipManiaShell.msix

# vite 가 설정을 로드할 때 만드는 임시 파일(빌드 부산물)
vite.config.js.timestamp-*
